<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude database files from backup -->
    <exclude domain="database" path="feminine_voice_database" />
    <!-- Exclude audio recordings from cloud backup -->
    <exclude domain="file" path="recordings" />
</full-backup-content>
